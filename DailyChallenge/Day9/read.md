A circular profile image (using `border-radius: 50%`) centered inside a square card with a gradient background.

| **03**  | If/Else          | **Exam Grader**          | Take a numeric score (0-100) as input. Print "Pass" if >75, "Fail" if <75. Add a special "Perfect!" message for 100.              |
| ------- | ---------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |