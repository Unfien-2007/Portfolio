to do list:

"Copy to Clipboard" Button: Let users copy the quote easily to send via text.

Background Change: Change the background color randomly every time a new quote appears.

Tweet Button: Add a link that automatically opens Twitter/X with the quote pre-filled.

add a database as source of quotes